{
  "name": "mixu_minimal",
  "description": "Minimal router and client",
  "version": "0.0.1",
  "scripts": {
    "test": "lessc style.less > style.css",
    "start": "http-server"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "less": "~ 2.7.2"
  }
}
